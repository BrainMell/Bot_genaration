# Binaries
image-service
image-service.exe
test-build

# OS files
.DS_Store
Thumbs.db

# Go stuff
go.sum
vendor/
# compiled binaries
*.exe
*.out
*.bin
image-service-linux
changes
# runtime / build artifacts
*.mp3
# Secrets - never commit env files or the pm2 config (it inlines tokens)
.env
.env.*
!.env.example
ecosystem.config.js
!ecosystem.config.example.js

# Compiled service binaries (20MB each, rebuilt on deploy)
bot-generation
bot-generation-new
bot-generation-test

# Editor/scratch backups
*.bak
*.bak.*
*.bak-*
